# Phone videos

Drop the shorts that should play on the phone at the start of the game into this folder,
then point an entry in `js/videos.js` at them, e.g. `src: 'videos/clip1.mp4'`.

Until a video is added, that short shows a black screen.
